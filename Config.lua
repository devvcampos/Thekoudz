ESP = {
    Enabled = false,

    DrawBox = true,
    DrawName = true,
    DrawHealth = true,
    DrawDistance = true,

    BoxColor = Color3.fromRGB(255, 255, 255),

    -- NOVO
    VisibilityCheck = true,
    VisibleBoxColor = Color3.fromRGB(0, 255, 0)
}
